import { ArchivedList } from '@/features/messages';

// Archived conversations — pushed over the tabs (Telegram-style folder), reached
// from the 'Archived' row at the top of the Messages list.
export default function ArchivedScreen() {
  return <ArchivedList />;
}
