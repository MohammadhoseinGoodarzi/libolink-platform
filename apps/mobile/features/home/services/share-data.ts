// DM friend grid source for the Share sheet. Per the handoff, the DM friends are
// the same people as the @mention suggestions, so reuse that list rather than
// duplicate it.
export { MENTION_USERS as DM_FRIENDS } from './comments-data';
